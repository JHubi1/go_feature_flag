## 0.1.0

- Initial release: `GoFeatureFlagProvider`, an OpenFeature provider for
  [GO Feature Flag](https://gofeatureflag.org) supporting the REMOTE
  evaluation mode (OFREP evaluation against a relay-proxy, client-side
  caching with ETag-based invalidation, and data collector/tracking event
  export).
